# Codex turn-state 缓存与观测

账号创建或编辑页可开启 Codex turn-state 缓存，选择个人、Team / Business 或按已有套餐自动识别，并设置有序的独立采集代理列表。默认关闭；仅支持常规 OpenAI OAuth。Spark 影子账号继承凭据母账号的设置和状态。未知套餐暂停缓存注入与采集，可手动选择分类。

全局生效模型名单默认是 `gpt-6-astra`、`gpt-5.6-sol`。`gpt-5.6-terra` 已移出默认名单，已保存的自定义名单不会被覆盖；管理员仍可手动加入其他模型。名单按最终上游模型 ID 精确匹配。

## 获取与续期

- 自然获取优先：成功交付的业务响应返回符合目标形态的状态后直接保存；满足采集或续期需求后取消同账号、模型的采集。
- 对应套餐的合法扩展形态触发采集：个人 312 字符 / 11 块，Team / Business 356 字符 / 13 块。没有状态、无效封装、套餐不匹配和未交付响应不建立新采集需求。
- 开启缓存时，最近 30 分钟内、当前凭据且成功交付的异常业务历史可触发采集。历史摘要不保存 token，正常历史不能变成可注入缓存。
- 本地有效期为签发后 60 分钟，到期前 5 分钟续采。重复收到同一状态不延寿；续期结果仍位于临期窗口时继续重试。采集未取得新目标时，仍有效的旧缓存继续使用。
- 仅“本次响应未取得目标形态”（包括同一临期状态未完成续期）等待 30 秒再试。连接、超时、HTTP 或流内普通错误不附加这 30 秒，下一次每秒调度即可重试；实际发送仍受队列、账号单飞及已有冷却约束。429 遵守 Retry-After 和账号冷却；401 / 403 暂停至凭据或配置变化。
- 仅维护最近 30 分钟有真实业务的模型，采集不延长这个窗口。空闲后结束需求；恢复使用先发正常业务请求。

采集与正常业务并行。已有异常需求或临期续期不等待业务请求结束，业务开始也不取消采集；业务返回同一临期 token 时继续续采。仍保留每凭据账号最多一个采集任务、限流退避和配置代次保护。并发结果按缓存内容与版本 CAS 校验，采集仅更新排队或退避状态不会使业务缓存快照失效；重复异常业务不会挤掉较新的合格采集结果，迟到异常和采集结果不能覆盖业务新取得的目标状态。

业务使用正常缓存出站后，成功交付的合法异常响应会废除这次请求使用的旧缓存并建立采集需求，一次即可触发。WS 响应先完成、发送成功回调后完成时，也会在确认发送后补执行这一动作；补偿只保留已验证的形态摘要，不保留响应 token。期间其他请求取得的新缓存受到保护，重复回调不会重复失效。新需求立即排队，但实际采集仍遵守已有退避和账号冷却。

短暂存储错误或连续 CAS 冲突时，正常完成和 WS 迟到绑定共用有界的待发布队列，按原请求的缓存身份重试失效与需求写入，不能清除并发取得的新缓存。队列最多 4096 项，每秒处理最多 32 项，每批预算 3 秒；过期、原业务发送满 30 分钟或配置不再适用后停止补偿。队列只在本进程内保存必要的私有比较证据，不保存响应 token 或业务正文，也不能用于注入；尚未成功落库的项在重启后不恢复，已落库需求仍由 PostgreSQL 恢复。

仅更换采集代理时，保留同凭据、同分类下符合目标形态且尚未过期的缓存（个人 292 / 10 块、Team / Business 332 / 12 块），继续使用至原到期时间，再通过新代理采集；这份保留缓存不提前五分钟续采，也不延寿。配置代次仍更新，旧代理在途结果不能回写。异常、过期状态的既有采集需求继续使用新代理处理并遵守原退避；空缓存不会仅因换代理新增需求。之后业务若返回有效异常形态，旧缓存失效并恢复采集；若返回新目标 token，则按新 token 的常规五分钟续期规则维护。取消采集代理后仍保留有效缓存，到期后仅自然学习。单账号编辑与批量配置采用相同行为，其他模型不会因这一缓存等待到期而被额外冷却。

每次采集使用当前 OAuth 凭据、最终模型和指定采集代理，向 Responses 接口发送一条独立短请求，超时 20 秒。采集不使用业务正文、原 turn-state 或 continuation，不进入插件转发、模拟遥测、每日会话根或下游计费。采集是上游模型请求，可能消耗账号额度。

采集 JSON 正文对齐 `ccodex-sleep-state@b18fabf9`：instructions 和用户消息均为 `Reply with OK.`，消息明确标记 `type: message`，设置 `stream: true`、`store: false`、`parallel_tool_calls: true` 及 `include: ["reasoning.encrypted_content"]`。不指定 reasoning effort，使用上游默认值；账号身份头和独立采集请求 ID 仍由本项目生成。

HTTP 429 和 SSE 失败事件中的结构化错误码 `rate_limit_exceeded` / `insufficient_quota` 均记为采集限流，沿用账号级退避。SSE 限流保留真实 HTTP 状态，不从错误文本猜测；重试遵守响应头 Retry-After 及账号已有冷却，不再额外设 30 秒下限。失败响应中的 token 不发布为缓存，已有有效缓存和脱敏观测按原规则保留。

未配置采集代理时仅从业务响应学习；代理不可用时显示失败原因，不切换到业务出口。没有可用缓存时照常转发业务，不等待采集、不阻断、不重放。

## 多代理轮换

采集代理列表的顺序就是轮换顺序。每个凭据母账号、最终上游模型独立保存当前代理与异常累计次数，首次从第一条开始。只有一次成功完成的独立采集返回经封装、时间及套餐校验的个人 312 / 11 块、Team / Business 356 / 13 块，且没有有效目标候选时，才累计一次；一个请求中的多个异常候选也只计一次。业务响应的异常不计入代理次数。

当前代理累计第三次有效异常后切到下一条并归零，末尾回到第一条；下一次仍遵守至少 30 秒重试间隔和更长冷却，不立即额外发送。超时、连接失败、无状态、无效封装、套餐不匹配等结果既不增加也不清零，429 仍退避，401 / 403 仍暂停。网络故障不会触发跳过代理。仅有一条代理时继续使用该代理，每满三次重新计数；空列表表示仅自然学习。

业务或采集取得满足需求的目标状态后，清零该模型计数并保留当前代理供下次使用。相同临期 token 没有完成续期时不清零；空闲结束需求时保留代理与计数，恢复活跃后继续。轮换位置和计数保存在 PostgreSQL，重启与跨实例不会重置；每次采集的私有尝试标识阻止重复回调或迟到结果重新计数、切换代理。

自动轮换只更新运行态，不改变配置代次、不清除有效缓存，也不改变常规提前五分钟续期。管理员增删代理或调整顺序时，更新配置代次、撤销旧尝试，从新列表第一条重新计数；正常有效缓存仍按上一节的代理变更规则保留至原到期时间，异常需求继续并保留必要退避。

账号配置新增 `collector_proxy_ids` 有序数组。旧 `collector_proxy_id` 仍可读取为单元素列表；显式数组优先，`[]` 表示清空。新写入使用数组，已有多代理账号拒绝仅用旧单值字段覆盖。列表中的每条代理都受到引用计数和删除保护。导出使用 `codex_turn_state_proxy_keys`，导入按顺序映射且兼容旧单键；映射不完整时该账号导入失败，运行态和计数不参与迁移。

## 观测与页面

轻量观测独立于缓存开关和指纹观测开关，每模型保留本实例最新实际返回的状态摘要。业务与独立采集均可更新摘要，详情标明请求来源及响应头 / metadata 载体。没有返回状态的请求保留原摘要和原时间。重启会清空这些本实例摘要；已加密保存的缓存和采集需求仍在数据库中。

摘要按真实发送时确认的凭据代次隔离。替换凭据后不展示旧凭据的红绿状态，旧请求迟到也不能覆盖新凭据摘要。修改个人／Team 分类后，单账号及批量接口依据观测时已验证的封装重新匹配当前分类；不从长度反推套餐，不把当时无效的状态变成有效状态，也不改变历史观测时间。

账号列表优先按生效名单显示“状态点 + 模型名”，最多 5 行；不足 5 行时，其他模型产生实际观测后动态补入，不留占位行。超过上限的模型在详情查看。独立采集缓存的以下生命周期提示优先于历史观测：仍可用但将在 5 分钟内过期时显示黄色；已经过期，且后台明确判定该模型超过 30 分钟无业务而停采时显示灰色。悬停可查看具体原因。其余情况依据最新观测；没有观测时参考当前有效缓存：

- 绿：符合对应套餐的目标形态。
- 红：已观测到对应套餐的有效扩展形态。
- 灰：没有状态、无法分类或无法可靠判断。

缺缓存不会自动显示红色；已知异常正在重试时仍为红色。刷新期间保留内容和颜色，收到新结果后更新。弹窗桌面端左列缓存、右列最近观测，小屏上下排列；打开时每 5 秒刷新。“最早可重试时间”不是保证发送时间，仍受采集单飞、队列和账号状态约束。业务在途不再显示“等待业务请求完成”。采集暂停不代表仍有效的缓存不能使用。

除上述黄色临期／灰色空闲过期提示外，状态点表示最近观测的形态，不能直接当作缓存可用性：如果仍有正常缓存，但最近业务响应是 312 / 356，列表仍可显示红色。业务观测中的出站长度来自该物理请求的实际请求头／正文；长度 0 表示该请求没有携带 turn-state。收到异常响应头也不等于满足采集触发条件，HTTP 失败、未成功交付或被丢弃的响应可以留在轻量观测中，但不会建立采集需求。

右侧显示这条历史观测对应请求的发送时间、注入动作、缓存来源、未注入原因和业务交付结果，不与左侧最新业务时间拼接。独立采集的出站 0 显示为“未携带（独立采集）”，业务出站 0 显示为“未携带”；独立采集本身不携带旧状态。关联详情可查看为诊断单独生成的观察 ID、实际注入快照的版本及原到期时间，不公开运行态锁、凭据或配置代次。旧摘要缺少这些字段时不推测补齐。

采集详情按固定错误类别显示原因和排查提示，区分连接失败、空响应、响应流读取失败、上游流内失败、响应未完成、事件过大、HTTP 请求被拒绝和上游服务异常。代理认证、代理不可用、超时、限流及鉴权或访问拒绝分别说明；连接失败本身不能证明代理并发不足。账号不可采集时区分状态非正常、调度关闭和账号过期。相同的采集原因与最近错误合并展示；不同的历史错误标明为上次采集错误。已有通用“采集失败”记录无法追溯细分类别，下次采集后更新。接口与页面只使用固定的脱敏类别，不展示原始错误、代理地址、凭据或响应正文。

新的网络诊断进一步区分 DNS、连接拒绝／关闭、TLS、可识别的代理认证／隧道失败，以及连接、TLS 握手、等待响应头超时。分类来自类型化错误、传输阶段及依赖的少量精确固定错误模板，不按任意错误文本猜测。总截止时间到达时保留已知的阶段和 SSE 限流原因；无法识别时仍显示通用失败。采集上限仍为 20 秒，只有未取得可用目标状态的结果使用固定 30 秒重试间隔，代理轮换计数规则不变。实际发送前再次检查账号是否正常、可调度且未到期。

安全日志 `openai_codex_turn_state_observation_completed` 记录同次请求的关联 ID、发送与观测时间、实际出站长度、注入快照版本和交付标记；`openai.codex_turn_state_collector_transport_failed` 记录采集耗时、固定阶段与错误码。日志均不记录 token、可恢复片段、哈希、代理凭据或错误原文。没有状态的业务响应不覆盖页面上的旧观测，但仍可用其独立完成日志核对出站注入。

每模型详情还显示当前／下次采集代理、最近尝试代理，以及该代理异常累计 0 / 3、1 / 3、2 / 3。累计次数不代表所有请求失败次数；第三次有效异常完成轮换后，新代理从 0 / 3 开始。账号列表继续只显示状态点与模型名。

## 存储与升级

迁移 `245_openai_codex_state_credential_epoch.sql` 初始化私有凭据代次；`246_openai_codex_state_demand.sql` 增加持久化需求、采集阶段和历史消费水位；`247_openai_codex_state_collector_rotation.sql` 保存按模型的采集代理、异常计数、最近尝试代理及私有尝试标识。沿用现有迁移机制应用。普通用量更新不会使缓存失效；凭据或缓存配置变化受行锁和版本保护。私有代次、运行态和 token 不参与账号导入导出。

PostgreSQL 是缓存权威来源，Redis 用于跨实例互斥、取消和配置激活通知。每实例最多 4 个采集 worker，每个凭据账号跨实例最多一个采集任务。存储不可用时恢复原业务行为，不依赖旧本地缓存继续注入。

状态接口 `GET /admin/accounts/:id/codex-turn-state` 及批量接口仅返回脱敏摘要。模型状态增加 `cache_available`、`collection_status`、`collection_reason`；观测增加 `request_source`（`business` / `collector`），与 `response_source`（响应载体）分开。多代理状态顶层返回 `collector_proxy_ids`，每模型返回 `collector_proxy_id`、`last_collector_proxy_id` 和 `collector_extended_count`；私有尝试标识不返回。页面读取不会触发采集。

观测诊断字段均为兼容性可选字段：`request_sent_at`、`outbound_action`、`outbound_source`、`maintenance_reason`、`business_delivered`、`observation_id`、`snapshot_version`、`snapshot_expires_at`。交付标记只用于已完成的业务请求；采集不伪造业务交付，快照字段只描述实际注入的冻结快照。本次诊断修复不新增数据库迁移。

长度和块数是加密封装的可观察形态，不是 HTTP 状态码，也不证明模型质量。系统检查 Base64url、Fernet 封装及签发时间，不解密或验证未知密钥的签名；60 分钟是本地维护策略。
